<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Vòng Quay May Mắn</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      text-align: center;
      margin: 0;
      padding: 0;
      background: #f7f7f7;
    }

    h1 {
      margin-top: 20px;
      color: #333;
    }

    #nameInput {
      margin: 10px;
      padding: 10px;
      width: 250px;
    }

    #addName {
      padding: 10px 20px;
      background: #28a745;
      color: white;
      border: none;
      cursor: pointer;
    }

    #spinWheel {
      margin-top: 20px;
      padding: 10px 30px;
      background: #007bff;
      color: white;
      border: none;
      font-size: 16px;
      cursor: pointer;
    }

    #namesList {
      margin-top: 15px;
      font-style: italic;
    }

    #winner {
      margin-top: 30px;
      font-size: 24px;
      font-weight: bold;
      color: #d9534f;
    }

    .wheel {
      margin: 30px auto;
      width: 300px;
      height: 300px;
      border: 10px solid #ccc;
      border-radius: 50%;
      position: relative;
      overflow: hidden;
      transition: transform 4s ease-out;
    }

    .wheel .segment {
      position: absolute;
      width: 50%;
      height: 50%;
      top: 50%;
      left: 50%;
      transform-origin: 0% 0%;
      border: 1px solid #333;
      text-align: right;
      padding-right: 10px;
      font-weight: bold;
      color: #333;
      display: flex;
      align-items: center;
      justify-content: flex-end;
    }

    #pointer {
      width: 0;
      height: 0;
      border-left: 20px solid transparent;
      border-right: 20px solid transparent;
      border-bottom: 30px solid red;
      margin: auto;
      margin-top: 20px;
    }
  </style>
</head>
<body>

  <h1>🎉 Vòng Quay May Mắn 🎉</h1>

  <input type="text" id="nameInput" placeholder="Nhập tên người chơi" />
  <button id="addName">Thêm</button>

  <div id="namesList">Danh sách: (chưa có ai)</div>

  <div id="pointer"></div>
  <div class="wheel" id="wheel"></div>

  <button id="spinWheel">Quay!</button>

  <div id="winner"></div>

  <script>
    const nameInput = document.getElementById('nameInput');
    const addNameBtn = document.getElementById('addName');
    const namesListDiv = document.getElementById('namesList');
    const wheelDiv = document.getElementById('wheel');
    const spinBtn = document.getElementById('spinWheel');
    const winnerDiv = document.getElementById('winner');

    let names = [
      "Quỳnh Anh", "Minh Thư", "Ngọc Anh", "Thanh Hương",
      "Nhã Kỳ", "Quỳnh Nga", "Gia Ngân", "Khánh Ngọc",
      "PHƯƠNG THÙY", "Bảo Trân", "Bích Tuyền", "Tường Vy"
    ];

    let currentRotation = 0;

    function updateNamesList() {
      if (names.length === 0) {
        namesListDiv.textContent = 'Danh sách: (chưa có ai)';
      } else {
        namesListDiv.textContent = 'Danh sách: ' + names.join(', ');
      }
    }

    function renderWheel() { wheelDiv.innerHTML = '';
      const angleStep = 360 / names.length;

      names.forEach((name, index) => {
        const segment = document.createElement('div');
        segment.className = 'segment';
        segment.style.transform = `rotate(${index * angleStep}deg) skewY(-${90 - angleStep}deg)`;
        segment.style.background = index % 2 === 0 ? '#f9c74f' : '#90be6d';
        segment.textContent = name;
        wheelDiv.appendChild(segment);
      });
    }

    addNameBtn.addEventListener('click', () => {
      const name = nameInput.value.trim();
      if (name && !names.includes(name)) {
        names.push(name);
        nameInput.value = '';
        updateNamesList();
        renderWheel();
      }
    });

    function spinWheel() {
      if (names.length === 0) {
        alert('Bạn chưa thêm tên nào!');
        return;
      }

      const randomIndex = Math.floor(Math.random() * names.length);
      const degreesPerSlice = 360 / names.length;
      const targetRotation = 360 * 5 + (360 - randomIndex * degreesPerSlice - degreesPerSlice / 2);
      currentRotation += targetRotation;

      wheelDiv.style.transition = 'transform 4s ease-out';
      wheelDiv.style.transform = `rotate(${currentRotation}deg)`;

      setTimeout(() => {
        winnerDiv.textContent = `🎉 Người chiến thắng là: ${names[randomIndex]}! 🎉`;
      }, 4000);
    }

    spinBtn.addEventListener('click', spinWheel);

    // Khởi tạo
    updateNamesList();
    renderWheel();
  </script>
</body>
</html>
